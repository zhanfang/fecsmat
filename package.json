{
    "name": "fecsmat",
    "displayName": "fecsmat",
    "description": "A fecs format extension",
    "version": "0.0.2",
    "publisher": "zhanfang",
    "engines": {
        "vscode": "^1.5.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.format"
    ],
    "main": "./src/extension",
    "contributes": {
        "commands": [
            {
                "command": "extension.format",
                "title": "fecs format"
            },
            {
                "command": "extension.sayHello",
                "title": "hello world"
            }
        ],
        "keybindings": [
            {
                "command": "extension.format",
                "key": "cmd+shift+r"
            }
        ]
    },
    "scripts": {
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "@types/mocha": "^2.2.32",
        "@types/node": "^6.0.40",
        "eslint": "^3.6.0",
        "mocha": "^2.3.3",
        "typescript": "^2.0.3",
        "vscode": "^1.0.0"
    },
    "dependencies": {
        "vinyl": "^2.1.0"
    }
}
